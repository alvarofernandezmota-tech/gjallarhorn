"""Donde se escribe y como, para que no se pierda nada.

Se llama asi y no `datos` porque `datos/` es la carpeta de los
datos de verdad, que esta en el .gitignore: un paquete con ese
nombre no se subiria.
"""
